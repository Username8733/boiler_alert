# Home Assistant

`automation.yaml` innehåller notiser och loggning. Byt `notify.mobile_app_DIN_TELEFON` till din faktiska notify-tjänst.

`dashboard.yaml` är ett enkelt Lovelace-kort för larm, LED-nivå, tröskelvärde och kvittering.
