# Modular Firmware Source

The recommended installation file is:

```text
firmware/release/boiler-monitor.yaml
```

The `source` directory is reserved for future modular development. The release candidate deliberately keeps the production firmware in one file to simplify installation and review.
